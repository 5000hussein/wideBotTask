# Normalise line endings so the repo is clean on every platform.
* text=auto eol=lf

# Binary assets — never touch these.
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
*.ttf binary
*.eot binary
